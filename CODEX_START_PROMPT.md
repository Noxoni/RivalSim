# Active Codex Start Prompt — RivalSim v0.2

The v0.1 GPU physics proof is complete and frozen.

The active implementation package is:

`handoff/v0.2/CODEX_START_PROMPT.md`

Codex: read that file and every document it requires, then execute RivalSim v0.2 completely. Implement, benchmark, parity-check, document, commit and push the v0.2 arena + ground-contact proof.

Do not modify published `results/v0.1/` evidence and do not begin v0.3 dynamic contacts in the same run.
