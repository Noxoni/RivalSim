# Prospective correction of stale standard-kickoff wheel contacts

Freeze this plan before the native corrected-boundary validation. The immutable
+300 trace and wheel-only inference intervention establish the specific fault;
they are not a checkpoint selection exercise.

1. Invalidate the eight per-world wheel-contact cache flags only for worlds
   receiving a standard physical kickoff reset. Put the write in the existing
   native reset kernel. Do not change contact physics, reward, policy, optimizer,
   Nexto scheduler, reset timing, or non-reset world state. Zero means the same
   invalidated pre-physics cache as constructor and current curriculum resets;
   subsequent physics computes actual contacts.
2. Native focused tests must poison old wheel flags and verify masked clearing,
   preserved unselected worlds, all five kickoff layouts/both sides, and
   deterministic policy parity with fresh kickoff inputs. Use zero learning.
3. Compare the entire post-reset Direct Skills state and observations before and
   after correction on a fixed small native fixture. The scenario reset already
   clears wheel flags: require bit-identical training reset state. Preserve the
   original fixture before editing the kernel.
4. Run the existing native goal/truncation/reward checks and CPU checks. Keep the
   original authority/package/evaluation evidence. Bind the corrected runtime
   through an explicit operational package amendment, publish and read it back
   before training resumes. No reward or PPO authority change.
5. Preserve old full-match evaluations as old-method evidence; standard match
   reset semantics improve and must be explicitly versioned. A corrected paired
   parent/latest evaluation is necessary before attributing later evaluation
   movement to training. Do not overwrite old snapshots/evaluations or compare
   old versus corrected scores as pure learning progress.
6. Resume latest accepted +451 model/Adam/RNG only. The shared goal is not met;
   do not deploy or claim SSL, shooting or possession mastery from this fix.
