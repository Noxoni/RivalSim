# Rival 120 Hz human-demo dataset V1

This directory freezes source references, whole-attempt and lifecycle-region splits, read-only adapter exactness evidence, and balanced-sampling metadata. No training was performed. `adapter_audit.json` is fail-closed: the committed native recorder does not contain enough state to emit any exact 182-field observation.
