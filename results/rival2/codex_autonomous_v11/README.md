# Codex autonomous V11

V11 performs a tiny fixed-seed antithetic search around the V10 checkpoint.
Only analog actor-mean parameters move. Each candidate receives the full
deterministic Nexto and unchanged human-validation gates, and the immutable
V10 source remains the fallback.
