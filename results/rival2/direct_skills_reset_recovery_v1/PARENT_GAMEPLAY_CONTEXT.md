# Match improvement is not broad ground-play competence

This is a deterministic CPU reduction of previously committed evaluations, not
another simulation run, policy forward pass, optimizer step or learning change.
It verifies source/checkpoint hashes and the original skill-bank hash before
grouping cases by their initial state. Thirteen focused CPU tests pass, including
actual first-checkpoint accounting and corrupted/misbound metadata rejection.

The selected +600 parent has these distinct results:

| Context | Goals / concedes / timeouts | Interpretation |
|---|---:|---|
| Original natural kickoff subset,35 starts |35 /0 /0|Repeated standard layouts, not35 independent configurations|
| Original natural ongoing-ground subset,29 starts |3 /25 /1|Weak non-kickoff ground play;21cases contain a Rival touch|
| Original finishing,64 starts |6 /42 /16|Reaching the ball is not converting shots against an established keeper|
| Original challenge,64 starts |0 /38 /26|11controlled-acquisition proxy events do not mean strong match possession|
| Original defense,64 starts |8 /21 /35|Clear/control proxy events are not a won match|
| V3 full matches,ten kickoff-origin regulation games |169 /122 goals,8wins|Stronger measured development result, not SSL or native RLBot proof|

Skill cases end at first native goal or truncation; full matches repeatedly
reset through standard kickoff states. They are different distributions and
exposure lengths. The goal here is eventual broad gameplay, not only favorable
responses from standard kickoff states. Neither the raw score nor the aggregate
natural-family result can erase poor ongoing-ground and set-keeper outcomes.

The291recorded native full-match goal events have strictly increasing ticks
within each match, all valid goal-plane crossings, interval minimum5.21667s,
median8.29167s, maximum55.70833s. The trace does not show an immediate zero-time
goal/reset scoring loop. This narrow event check is not a complete physics or
opponent audit. Goal-entry height is recorded without calling it an aerial,
shot quality, intent, or identifying an unavailable last-toucher sequence.

The current25-update shooting-pressure branch continues unchanged. The same
CPU review helper is ready to audit its final checkpoint/counters/curve, compare
the original full matches and skill cases, and separate ongoing-ground from
kickoff outcomes. Do not substitute the easier training starts for evaluation.

Commands:

```
.venv/Scripts/python.exe benchmarks/report_direct_skills_reset_recovery.py parent
.venv/Scripts/python.exe benchmarks/report_direct_skills_reset_recovery.py review
```

The second command requires the actual terminal `complete_review` child25state,
immutable final checkpoint, complete25-row curve and completed evaluations.
It cannot manufacture completion while training runs. All rows are retained;
early1..12contains the declared fresh-episode transient, late13..25does not.
Rates use actual learner/role decisions, never source-start proportions.
